import { useState } from "react";

export type HelpCenterView = "dashboard" | "library" | "publish" | "skills" | "accounts" | "logs" | "help";

export function HelpCenter({ onNavigate }: { onNavigate: (view: HelpCenterView) => void }) {
  const [guideError, setGuideError] = useState("");

  async function openUserGuide() {
    setGuideError("");
    if (!window.contentFerry) {
      setGuideError("完整说明文件只能从文渡桌面应用中打开。");
      return;
    }
    try {
      await window.contentFerry.openUserGuide();
    } catch (error) {
      setGuideError(error instanceof Error ? error.message : "无法打开完整使用说明。");
    }
  }

  return (
    <div className="help-center">
      <section className="card help-hero">
        <div>
          <p className="eyebrow">快速上手</p>
          <h2>先完成配置，再写第一篇文章</h2>
          <p>文章保存在你自己的 VitePress 文章库；模型、账号和发布记录保存在本机。</p>
        </div>
        <span>
          <button className="secondary-button" onClick={() => void openUserGuide()}>打开完整说明</button>
          <button onClick={() => onNavigate("dashboard")}>开始创作</button>
        </span>
      </section>
      {guideError && <p className="error">{guideError}</p>}

      <section className="help-steps">
        <article>
          <span>1</span>
          <h3>连接模型</h3>
          <p>在“技能与模型”登录预置的 OpenAI Codex，或添加任意 OpenAI 兼容端点作为自定义连接（如 OpenAI API、OpenRouter、Nous Research、GitHub Copilot 等）。</p>
          <button className="text-button" onClick={() => onNavigate("skills")}>去配置模型</button>
        </article>
        <article>
          <span>2</span>
          <h3>选择文章库</h3>
          <p>选择 VitePress 的 <code>docs</code> 目录，不要只选择 <code>docs/posts</code>。草稿会直接写入文章库。</p>
          <button className="text-button" onClick={() => onNavigate("library")}>去归档库</button>
        </article>
        <article>
          <span>3</span>
          <h3>连接公众号</h3>
          <p>在微信公众平台“设置与开发 → 基本配置”获取 AppID、AppSecret，并把当前公网出口 IP 加入白名单。</p>
          <button className="text-button" onClick={() => onNavigate("accounts")}>去账号设置</button>
        </article>
        <article>
          <span>4</span>
          <h3>创作并发布</h3>
          <p>从主题开始，阿文先联网补充可追溯资料；之后可在“编辑创作方向”修改主题、目标、读者、角度和资料。再生成提纲与正文；完成封面与 AIGC 特征检测后，先同步微信草稿并人工预览，再决定普通发布或群发。生成提纲后可以先点“暂存提纲”，下次打开会恢复暂存版本；只有“确认提纲并标记为已就绪”才会推进项目状态和正式同步标题，“放弃本次修改”不会保存本次修改。如果对结构或标题不满意，可在提纲编辑器下方使用“和阿文一起优化”：结构模式先预览完整提纲建议，标题模式返回候选标题；选择标题只更新当前编辑内容，不会静默覆盖当前内容。</p>
          <button className="text-button" onClick={() => onNavigate("dashboard")}>新建文章</button>
        </article>
      </section>

      <section className="card help-faq">
        <h2>微信公众号配置</h2>
        <details>
          <summary>AppID 和 AppSecret 在哪里获取？</summary>
          <p>登录 mp.weixin.qq.com，进入“设置与开发”，打开“基本配置”或“开发接口管理”。AppSecret 通常只在生成或重置时完整显示，请立即保存到密码管理器。</p>
        </details>
        <details>
          <summary>IP 白名单应该填写什么？</summary>
          <p>填写运行文渡电脑的公网出口 IP，不是 127.0.0.1 或 192.168.*。动态公网 IP 变化后，需要在公众号后台重新修改白名单。</p>
        </details>
        <details>
          <summary>为什么还要配置回调？</summary>
          <p>微信接口返回提交成功不代表最终发布成功。公众号需要通过公网 HTTPS 地址把最终事件转发到文渡的 <code>/wechat/callback/&lt;账号 ID&gt;</code>。</p>
        </details>
        <details>
          <summary>原创、赞赏和合集怎么设置？</summary>
          <p>在文章编辑页的“文章设置”中可勾选“文章由 AI 生成”、申请原创声明、开启赞赏，或填写合集名称。勾选“文章由 AI 生成”后，发布记录进入“在微信后台完善并发布”时，文渡会尝试把微信编辑器的“创作来源”选择为“内容由AI生成”；这只是你的发布来源声明，不等同于 AIGC 检测结论。草稿创建后，在发布记录点击“在微信后台完善并发布”；文渡会先读取微信“选择合集”窗口中可见的名称，按公众号缓存后再匹配文章设置。下次可直接从同一公众号的下拉建议中选择；微信后台仍是最终来源。你仍要核对最终效果，并在微信后台点击发布。</p>
        </details>
        <details>
          <summary>从工作台进入“设置并发布”后，为什么已选好公众号？</summary>
          <p>若这篇文章还没有保存发布账号，文渡会自动选中第一个已绑定的微信公众号账号，避免再多一步选择。已保存的账号不会被自动替换，你也可以在文章设置中随时改选。</p>
        </details>
        <details>
          <summary>「待处理」和「发布记录」有什么区别？校正状态还要注意什么？</summary>
          <p>「待处理」只列出尚未确认最终结果的任务；「发布记录」列出已确认终态的任务，包括已发布、已取消，以及你在平台后台核实后人工标记为失败的记录。CSDN、博客园、掘金和 51CTO 的终态记录可点击「状态轨迹」查看只读的统一生命周期事件，按时间回看排队、准备、提交、失败、人工核对和校正过程；查看轨迹不会重新提交平台任务。微信公众号继续使用自己的发布事件记录和人工校正流程。校正弹框的「最终状态」默认跟随任务当前状态，直接确认即可归档：失败任务确认「发布失败」后会出现在「发布记录」并显示红色「已失败」徽标与核实依据；系统自动判定失败但未经你核实的任务仍留在「待处理」，等待重试或核实。若不再发布，把「最终状态」选为「取消发布」即可。</p>
        </details>
        <details>
          <summary>怎样创建 CSDN 渠道稿？</summary>
          <p>先添加一个 CSDN 账号，再到「工作台」对未归档文章点击 CSDN 平台按钮（已归档文章可在「归档库」点击「重新发布」，在发布设置里选 CSDN 账号后进入 CSDN 渠道稿）。可让阿文生成独立适配稿，也可直接使用主稿而不调用模型；之后可编辑并冻结版本。CSDN 稿不会包含公众号链接、文末延伸阅读或其他软引流。冻结后创建发布任务，再点「在浏览器中完成发布」：文渡会打开 CSDN 编辑器自动填充标题与正文，你在浏览器核对后点文渡的「我已在 CSDN 发布」，文渡会点击 CSDN 的发布按钮并读回链接；读不到链接时可在后台核实后「校正状态」。文渡不会绕过你的确认自动发布。</p>
          <button className="text-button" onClick={() => onNavigate("dashboard")}>去工作台</button>
        </details>
        <details>
          <summary>博客园的分类和标签怎样填写？</summary>
          <p>在博客园渠道稿的“发布设置”中，点击“读取博客园个人分类与 Tag”。文渡会打开博客园创作后台，使用该窗口已登录的账号读取个人分类和自定义 Tag；首次请先登录。两处都可以直接手工填写。网站分类不是个人分类，文渡不会用它代替。</p>
        </details>
        <details>
          <summary>补研资料不够时怎么补充？</summary>
          <p>工作台已有资料的文章会显示“查看资料”和“继续补研”；在全页资料工作台中，可以让阿文继续联网核查，也可只新增指定资料链接。系统会自动抓取指定链接并尝试生成资料卡，正常情况下不需要人工确认；只有登录、验证码、复杂网页交互、内容冲突或获取失败时才需要人工处理。搜索结果只负责发现候选，自动资料卡必须有正文核验摘录；卡片会说明可支持的主张、为什么值得看、质量/时效、证据边界、提取时间和内容指纹，并合并同质来源。AI 推荐和作者决定会分开显示：当前作者决定会用带颜色的状态按钮突出显示，其他按钮用于切换决定；选择“采纳”后资料才会进入提纲、正文和事实检查；待核验或拒绝的资料不会进入。若合并不准确，可在“资料卡纠错”中拆分来源或合并同一主张，来源和作者决定会保留。资料链接和归因只是写作候选，不会自动插到正文。需要复核会变化的资料时，点击“刷新易变事实”；它只处理产品能力、价格、规则、版本、限额和适用条件，不覆盖已有资料或作者决定。资料工作台工具栏中的“调研运行历史”可回看每次初始调研、继续补研和刷新当时的计划、结论与资料快照；失败、取消或暂停的运行也会保留原始要求、错误和检查点，失败或取消记录可点击“沿用原请求重试”，不必重新输入补研信息；不再需要处理时可点击“移除记录”，它只隐藏历史任务，不删除资料卡、结论或审计事件；三个辅助工具共用页面内面板，一次只展开一个，不会遮挡资料卡。资料工作台会显示本次问题、证据维度、时效/冲突提醒、已覆盖材料和缺口；应用会按未覆盖维度继续检索，模型自己说“够了”不会提前结束。快速核实、均衡调研、深入研究只改变执行预算，预算耗尽或仍有缺口时会明确显示“部分调研”，可直接围绕缺口继续补研。指定链接会显示自动获取状态；正文提取成功时会生成资料卡，资料卡仍需作者决定后才会作为写作依据。若获取失败，可以打开来源、重试补研或使用“手工补录资料卡”。联网失败时，界面会区分网页验证、网络/TLS 连接和服务异常；只有需要网页验证时才需要打开辅助浏览器，网络/TLS 错误应先重试或检查检索代理。补研等待较久时可点“暂停补研”，稍后再点“继续补研”；也可停止本轮任务。已保存的资料卡和历史运行不会丢失；如果应用中途重启，重新打开资料工作台会自动接管排队或已失联的补研任务。工作台左侧导航顶部的箭头可收缩或展开；收缩后保留放大的图标入口，悬停可查看名称。</p>
        </details>
        <details>
          <summary>生成正文时会怎样使用资料卡？</summary>
          <p>只有你明确“采纳”的资料卡会进入正文生成。阿文会按提纲章节使用卡片里的具体事实、做法、评价、限制或反例，并遵守质量、时效和证据边界；待核验或拒绝的卡不会作为写作依据。资料卡不会自动变成正文中的 URL、脚注或资料列表。</p>
        </details>
        <details>
          <summary>怎样配置掘金账号的 Cookie？</summary>
          <p>在「账号」页添加掘金账号后，点击「配置掘金凭据」。推荐点击「自动获取 Cookie」：文渡会弹出掘金登录窗口，用扫码或手机号验证码完成登录后，会自动抓取 Cookie、AID（默认 2608）和 UUID 并回填保存，还会调用掘金接口验证登录态；验证未通过时凭据仍会保存，但界面会提示检查登录态。也可以手动从浏览器开发者工具复制含 sessionid 的 Cookie 粘贴保存。凭据加密保存在本机，不会回显。</p>
        </details>
        <details>
          <summary>怎样创建掘金渠道稿？</summary>
          <p>先添加一个掘金账号并配置凭据，再到「工作台」对未归档文章点击掘金平台按钮（已归档文章可在「归档库」点击「重新发布」，在发布设置里选掘金账号后进入掘金渠道稿）。可让阿文生成独立适配稿，也可直接使用主稿而不调用模型；直接使用主稿时会先打开渠道稿，分类和标签先按规则填充，AI 标签推荐在后台完成，不会阻塞编辑。之后可编辑并冻结版本。冻结后创建掘金发布任务，文渡会先以“排队”保存任务，再由后台运行器创建掘金草稿；草稿就绪后展示链接，确认无误后点击「确认公开」才会正式发布。掘金稿不会包含公众号链接、文末延伸阅读等软引流内容。</p>
          <button className="text-button" onClick={() => onNavigate("dashboard")}>去工作台</button>
        </details>
        <details>
          <summary>怎样创建 51CTO 渠道稿？</summary>
          <p>先添加一个 51CTO 账号并配置 Cookie，再到「工作台」对未归档文章点击 51CTO 平台按钮（已归档文章在「归档库」点 ⋯ 展开后点「重新发布」，在发布设置里选 51CTO 账号即可进入渠道稿）。在渠道稿的「发布设置」里先点「加载分类」从 51CTO 拉取你的栏目与分类，然后依次选择「一级栏目」和「授权分类」——两者是联动的，选好一级栏目后，二级授权分类只会列出该栏目下的分类，切换一级栏目时需要重新选择二级分类。再填写「标签」（必填，多个用逗号分隔）。分类拉取失败时也可以直接手动填写栏目 id / 分类 id。点「发布到 51CTO」后任务会先显示“排队”，再由后台运行器异步提交；不需要在浏览器里确认。若文章在 51CTO 后台被删除或需要换分类重发，改完后点顶部的「重新发布」即可，会用上次的分类和标签作为默认值。</p>
        </details>

        <h2>常见问题</h2>
        <details>
          <summary>编辑时流程图怎么是代码，能直接看图吗？</summary>
          <p>可以。<code>mermaid</code> 代码块在编辑器里会直接渲染成图，和 Obsidian 一样。<strong>点一下图</strong>就切回源码供你修改，把光标移到别处又会自动变回图。</p>
          <p>图形还没写完整时会临时显示成代码块，写完即自动变回图；如果语法有误，代码块下方会说明原因，内容不会丢失。存回文件的始终是 <code>```mermaid</code> 代码块本身，不会变成图片，方便你继续用其它 Markdown 工具编辑。</p>
        </details>
        <details>
          <summary>正文里的流程图和代码块，发布后会变成什么样？</summary>
          <p>发布到微信、CSDN、博客园、掘金和 51CTO 时，<code>mermaid</code> 代码块会先在本地渲染成 PNG 再上传，读者直接看到图片，不需要平台支持 mermaid；其它语言的代码块会转成各平台的代码样式。微信公众号只识别自己的代码块样式，所以不论原文标注什么语言，都按等宽字体、灰底的代码块呈现。</p>
          <p>如果图表没能转成图片，文渡会保留原始代码块，不会丢失内容。可在「运行日志」搜索“mermaid 渲染失败”查看不支持的语法和原因。</p>
        </details>
        <details>
          <summary>怎样给正文加高亮？发布后还会在吗？</summary>
          <p>用 <code>==</code> 把要强调的文字包起来，例如 <code>这是 ==重点== 内容</code>，和 Obsidian 的用法一致。所见即所得模式下直接显示黄底，保存回文件后仍是 <code>==重点==</code>，来回切换不会丢失。选中文字后按 <code>Ctrl+Shift+H</code> 也可以直接加高亮。</p>
          <p><code>==</code> 需要紧贴文字，<code>a == b == c</code> 这类写法不会被当成高亮；代码块和行内代码里的 <code>==</code> 也保持原样。发布时文渡会按平台能力自动转换，你看到的效果保持一致：微信公众号用带背景色的样式，其余平台用标准高亮标签，CSDN 直接使用原生的 <code>==</code> 语法。</p>
        </details>
        <details>
          <summary>技能与模型有什么区别？</summary>
          <p>技能决定任务规则，例如公众号撰写、去 AI 味和封面提示词；模型连接决定由哪个服务执行。腾讯朱雀和 ContentAny 使用浏览器自动化，不需要大模型。“联网检索服务”则独立于模型连接：可选配置 Tavily，以提升阿文补研公开资料时的稳定性。正文里要求阿文核实网址或官网时，文渡会先用应用侧检索服务抓取网页，再交给阿文分析；如果网络或代理导致抓取失败，阿文会明确说明未核验，不会假装已经访问。</p>
          <p>技能按模型依赖分成三组：文本类（创作、改写，用 OpenAI 系列）、图像类（用 ModelScope / Agnes AI）、无模型（朱雀、ContentAny 走浏览器自动化）。图片初审模型另有独立设置，可关闭、跟随当前阿文模型或指定模型连接；它复用已有凭据，不新增 Key。OpenAI Codex 当前支持图片输入，自定义连接会标为“待验证”，不支持图片输入的连接不能选择。打开文章左侧“图片素材”即可用 Tavily 搜索候选；每次搜索会按文章保存，点击“查看搜图历史”可以重新打开最近 30 次候选。配置 Codex 后会先临时做视觉初审，点击候选图可以看大图并打开原图；Tavily 返回源网页时还可以打开源网页。确认后才会下载并插入正文；阿文找图会推荐相关段落前/后的位置，无法匹配时回退到文章末尾。初审失败或未配置时仍保留候选供人工判断。你也可以直接对阿文说“找一张操作界面截图”或“给文章找配图”，阿文会调用受控找图工具并自动打开候选面板；候选仍需你核对并确认，不会自动写入正文。搜图或初审超时、关闭面板或切换文章时，旧结果会被取消或忽略，可以重新搜索。想给同一类里的部分技能统一换模型时，不必逐个点开编辑：先在技能卡片右上角勾选要改的技能，再在该组标题栏的“批量设置模型”里选好目标模型，点“应用到选中的 N 个技能”即可一次改完。</p>
        </details>
        <details>
          <summary>怎样让 AI 按我的要求改写一段文字？</summary>
          <p>先在正文中选中文字，选择“改写”“去 AI 味”等动作，再在“补充要求（可选）”中填写语气、保留项或避免项；留空则使用该技能的默认规则。按 Ctrl+Enter 可直接生成建议。结果会保存到与阿文的对话，可在对话中接受或拒绝；所见即所得模式的结构化工具栏会在停止交互约 1.8 秒后自动淡出，鼠标移到工具栏原来的顶部区域、点击、聚焦或输入即可显示，滚动正文和正文区域内普通移动不会触发显示。</p>
          <p>在所见即所得编辑区选中文字后，可用 Ctrl+C 复制；Ctrl+Z 和 Ctrl+Y 分别撤销和重做。</p>
          <p>长文章打开时，编辑器会先保证正文和首屏图片可操作，下面的图片会按需加载；SVG 图表会在本地后台转换并缓存，等待图片处理不会锁住正文滚动。</p>
        </details>
        <details>
          <summary>怎样让阿文分析 Git 源码或执行 Demo？</summary>
          <p>可以先到“技能与模型”设置“阿文专属工作区”。只给出公开仓库地址时，Git staging 会放在该目录下的 <code>git-sources/</code> 子目录；保存工作区即表示文渡可以在该目录内读写，越界目标会被拒绝。授权按钮和完整参数只在右侧“执行活动”显示一张授权卡，底部对话区只提供“查看执行活动”跳转，不会重复弹出同一授权。</p>
          <p>右侧“执行活动”中的“展开查看”会打开同一工作流的浮窗，方便查看完整时间线、参数、工具结果和回传内容；浮窗只是放大视图，不会新增另一套授权入口。</p>
          <p>首次起草正文前，阿文会先提出可编辑的最小实践计划。你可以按实际环境修改它、暂存后再继续，或明确选择不新增实践直接起草；计划中的预期不会被当成实测结果。</p>
          <p>在文章编辑器左侧打开“代码与工具”后会显示宽版执行面板。阿文在对话中发现需要时，也可以自动调用低风险的网页检索、系统工具发现和文章库只读；Git staging、写入目录或代码执行在工作区外会先显示授权卡，由你选择仅允许这一次、允许本次任务同类操作、允许当前文章同类操作、拒绝并让阿文重规划，或取消工作流。已配置的“阿文专属工作区”只对阿文自动创建的 Git staging 与分析提供固定目录边界，工作区内不重复询问目录授权，越界仍会拒绝。右侧“执行活动”会显示目标、参数、权限决定、事件时间线、工具结果、取消和重启恢复入口，避免把结构化数据直接渲染成“[object Object]”。只给出公开仓库地址时，阿文会请求应用在专属工作区的 `git-sources/` 下创建 staging 目录并展示实际目标；未配置工作区时使用受控临时目录，不需要你手动运行 `git clone`。每次运行都会显示实际目标、参数和目录，并留下执行记录；Git 摘要可在执行活动中填写主张并保存为“待核验证据”，之后可以单独采纳为证据卡，也可以把源码摘要插入正文引用，不能直接当作通用事实。授权目录可以保存为所有文章共享或仅当前文章使用，但项目租约默认 24 小时后失效，每次具体执行仍需重新确认，不会形成对未知命令的永久信任。工作流失败也会保留在执行活动中；如果工具已经完成但最终回复格式有误，阿文会在不重复工具调用的前提下自动修复有限次数，仍失败时显示失败原因和人工接管入口。应用重启后的恢复不会自动重放副作用。</p>
          <p>本机、WSL、Docker 和 Windows Sandbox 是不同执行目标，不可用时文渡会明确提示，不会偷偷回退到本机。安装依赖、删除文件和外部写入仍需另外确认。</p>
        </details>
        <details>
          <summary>文章、会话和阿文记忆保存在哪里？</summary>
          <p>文章保存在 VitePress 文章库；账号设置、发布记录、会话、记忆摘要和日志保存在文渡数据目录。长期记忆只保存提炼后的稳定写作偏好，不把完整会话直接当作写作规则。在正文底部的“与阿文讨论本文”面板中，“管理本文记忆”“收起”和“发送”位于右侧输入区底部的同一行，顶部不会再因为按钮单独预留一整行高度；点击管理可查看待确认候选、启用或删除当前文章的记忆，也可以导出 JSON 备份并合并导入；弹窗会把启用记忆、待确认候选、备份恢复和删除记忆分开，删除派生记忆不会删除文章正文。</p>
        </details>
        <details>
          <summary>归档后的文章还能看到当时的素材和提纲吗？</summary>
          <p>可以。在“归档库”点击文章右侧的 ⋯ 展开操作区，除“重新发布”外，还会按这篇文章留下的记录显示“查看创作方向”“查看素材”“查看提纲”。同一时刻只展开一行。不是通过文渡创建的文章没有这些记录，展开后只会显示“重新发布”。未归档的文章在工作台同样可查看：生成提纲或正文后，“编辑创作方向”会变为“查看创作方向”。</p>
        </details>
        <details>
          <summary>归档库里的文章怎么重新发到 51CTO、CSDN 等其他平台？</summary>
          <p>归档库渠道条中的“未生成”加号可以直接创建对应渠道稿，已有渠道稿按钮可以继续编辑；需要创建新的发布任务时，再点文章右侧的 ⋯ 展开操作区中的「重新发布」。重新发布会打开这篇文章并定位到右侧「发布设置」，「发布账号」决定发布去向，切换账号后配置项随之切换。</p>
          <p>选择微信公众号时，留言、原创声明、赞赏和合集等微信专属选项会直接显示，顶栏按钮为「准备发布」；选择 CSDN、博客园、掘金或 51CTO 时，面板会提示「在 XX 渠道稿中发布」，顶栏按钮变为「进入 XX 渠道稿」——这些平台的栏目、分类、标签、发布形式等平台专属设置都在渠道稿里填写，这里保存的作者、摘要和封面会带到渠道稿。点击「AI 生成适配摘要」等待模型返回时，仍可滚动、编辑正文和保存文章；只有摘要按钮会暂时禁用。</p>
        </details>
        <details>
          <summary>阿文已经处理过的建议为什么不会再出现？</summary>
          <p>接受建议后，它会先按建议标注执行：只有“替换原文”会替换，“追加到原文后”或“插入到原文前”会保留原文并插入独立段落；随后显示“已应用到当前草稿，尚未保存”。建议中的 replacement 必须是可以直接写入文章的正文内容，分析反馈和修改理由不会作为“接受追加”的内容。同一段落可以有多个互斥的替换或追加方案，接受其中一个后其他方案会自动拒绝。点击“保存文章”成功后才会显示“已应用并保存”。接受后可以继续润色已插入或已替换的文字，保存不会因为建议文本不再与原建议完全一致而让建议重新出现。对话中的待处理建议提供“定位”按钮，点击后正文编辑区会滚动到对应原文片段并短暂高亮。离开文章时如果还没有保存，文渡会让你选择保存并返回、放弃本次修改或继续编辑；放弃后建议会恢复为待处理。阿文对话会保留原建议、操作类型、建议文本和最终处理记录。建议卡可拖动，切换所见即所得和 Markdown 原文后会重新锚定到对应段落。</p>
          <p>如果你准备继续提问，而历史会话中仍有对应当前正文但尚未处理的建议，输入区会先提示数量。可以选择“批量拒绝并继续”“保留建议，继续提问”或“取消发送”；已应用到未保存草稿、已拒绝或已无法定位的建议不会被计入。</p>
        </details>
        <details>
          <summary>遇到错误怎么办？</summary>
          <p>到“运行日志”选择发生问题的日期，查看错误、请求路径、状态码和请求 ID。涉及微信发布时，可在发布记录核实后台结果并人工校正状态。</p>
          <button className="text-button" onClick={() => onNavigate("logs")}>查看运行日志</button>
        </details>
        <details>
          <summary>生成 AI 封面很久没有结果怎么办？</summary>
          <p>图片模型的排队时间可能较长，但不会锁定正文编辑。可继续编辑文章；若开启“AI 调用审计”，可在“技能与模型”页面给出的审计日志目录查看图片任务的提交、轮询和下载结果。</p>
        </details>
      </section>
    </div>
  );
}
