# ContentFerry 内容发布上下文

ContentFerry 负责把本地文章转换为不同平台的渠道稿并跟踪发布结果。本文件只记录稳定的业务词汇，具体实现和接口约束分别维护在 `spec/` 与代码中。

## 创作与发布

**指定资料**：用户在创建或补研内容项目时主动提供、要求纳入研究的网页或文件来源。系统必须保留其处理结果或明确失败原因；它不等同于可自由忽略的创作简报备注。
_避免_：已有资料备注（备注可包含观点或写作要求，不必是可抓取来源）

**证据卡**：围绕一个可用于文章论证的独立事实、观点或经验形成的可追溯资料单元，可关联一个或多个同质来源；不同证据性质即使结论相同，也分别保留。
_避免_：链接卡（URL 是证据卡的来源，不是卡片本身）

**调研覆盖**：创作简报拆出的待证问题，及其所需的官方事实、独立评价或实践、限制/反例等证据维度被可靠来源支撑的状态。它决定调研是否足够，不以资料卡数量作为目标。
_避免_：搜够几轮、凑够几张资料卡

**时效判断**：针对一项证据主张的时间适用性说明，标为当前可用、历史背景、需要复核或日期未知，并说明判断依据和边界。
_避免_：来源可信度总分（时效性不等同于权威性）

**部分调研**：因执行预算耗尽、指定资料尚待人工核验或存在已说明的证据缺口而未完全达到调研覆盖的结果。经用户确认后可用于后续创作，但缺口不得被当作已验证事实。
_避免_：调研完成（部分调研必须保留其未验证状态）

**主张归属**：证据卡中的每项可引用主张与一个或多个具体来源 URL 的可追溯关联；合并同质来源不能抹去这一关联。
_避免_：仅记录卡片级来源

**研究计划**：为当前创作简报列出的待证问题、所需证据维度、时效风险和待处理冲突的可见计划。它随选题动态生成，指导调研覆盖，不是固定来源分类清单。
_避免_：检索词列表（检索词只是执行研究计划的一种手段）

**证据冲突**：可靠来源对同一待证问题给出不兼容结论，或因版本、时间、适用范围不同而表面矛盾的状态。系统必须保留双方归属和边界，不能由 AI 静默消除。
_避免_：重复资料

**调研深度**：用户选择的执行时间和请求预算档位，可为快速核实、均衡调研或深入研究；它不改变调研覆盖的定义。
_避免_：资料卡数量设置

**证据快照**：一次成功读取网页后，为解释证据卡依据而保存的最小内容记录，包括提取摘要、关键片段、抓取时间和内容哈希；它不是网页全文存档。
_避免_：网页备份

**证据候选**：已完成正文核验或用户补录、可供用户选择用于创作的证据卡。搜索结果本身仅用于发现候选页面，不能直接作为证据候选。
_避免_：搜索结果

**证据采纳**：用户明确选择一张证据候选及其可用主张进入提纲、正文或事实检查上下文的操作。AI 推荐采用不等同于证据采纳。
_避免_：自动选中、资料卡推荐

**调研运行记录**：一次初始、继续或刷新调研的可回看执行结果，包含当时的研究计划、覆盖状态、证据快照、缺口和用户选择。后续调研可更新当前推荐，但不得覆盖该记录。
_避免_：当前资料（当前资料只是最新视图）

**渠道稿**：针对某一个外部平台准备的、可独立发布的文章版本，包含该平台需要的正文、标题、资源和发布选项。
_避免_：平台稿、临时稿（除非明确指未冻结的编辑草稿）

**发布任务**：用户针对一份渠道稿发起的一次可追踪发布尝试，拥有独立的状态、幂等标识和外部平台结果。
_避免_：发布记录（用于界面展示的历史视图，不等同于任务本身）

**冻结快照**：发布任务创建时确定的渠道稿内容、资源引用和渲染摘要；任务重试继续使用同一快照，重新发布才会创建新快照。
_避免_：主稿副本（冻结快照属于渠道稿，不是本地文章库的第二份正文）

**渠道适配器**：负责一个外部平台特有的内容转换、凭据、资源处理、提交和回执读取的实现。
_避免_：平台服务（当需要指代跨平台的生命周期逻辑时）

**人工校正**：当外部平台结果无法自动确认时，用户根据平台后台事实修正本地发布任务状态的操作。
_避免_：强制发布、回执覆盖

## 生命周期

**规范状态**：跨平台界面共同理解的发布任务状态；平台特有的原始回执和诊断信息作为附加详情保留，不改变规范状态的含义。
_避免_：把平台内部状态直接当作全局状态

规范状态至少包括：`queued`、`preparing`、`waiting_user`、`ready`、`submitting`、`published`、`needs_credentials`、`failed`、`needs_manual_reconciliation` 和 `cancelled`。终态任务不会被普通重试复用，重新发布会创建新的发布任务和冻结快照。

`waiting_user` 表示提交尚未完成，等待登录、浏览器确认或其他用户操作；`needs_manual_reconciliation` 表示请求可能已经到达平台，但远端结果无法自动判定，需要用户核实后校正本地状态。

## AI 协作与权限

**智能体权限**：用户授予阿文在特定范围内读取资料、调用工具、运行实验或修改内容的权限。权限按全局默认、项目授权和单次授权分层；更具体的授权不能覆盖明确拒绝，单次授权在任务结束、切换项目或应用重启后失效。

**工具**：阿文可以调用的一项有明确能力、输入、输出和副作用说明的外部能力，例如搜索、Git、音频处理、代码运行或 Mermaid 渲染。工具可被发现或注册不等于已经获得目录、网络、依赖安装、删除或发布权限。

**工具记忆**：关于工具路径、版本、已验证能力、成功环境和适用场景的可追溯经验。工具记忆只帮助阿文选择候选工具，不能替代当前任务的权限检查。

## 记忆

**原始事件**：用户消息、阿文回复、工具调用与结果、资料卡、权限决定、文章版本和建议处理结果等可追溯事实底稿。原始事件不等同于长期写作规则，也不能被摘要悄悄覆盖。

**候选记忆**：从原始事件中提炼出的、尚未确认适用范围或稳定性的记忆提案。候选记忆可以被合并、拒绝、过期或要求用户确认。

**长期记忆**：从原始事件和确认反馈中提炼出的、带来源和适用范围的稳定经验。它是可重建、可失效的派生层，不是文章正文、平台回执或已核实事实的第二份正式来源。

**记忆巩固**：在不阻塞当前对话的情况下，把近期事件和候选记忆整理为稳定长期记忆的过程，包括去重、合并、冲突处理、过期和归档。

**记忆使用记录**：一次生成、研究或工具决策实际引用了哪些记忆的可追溯记录。它用于解释结果和撤销影响，不代表这些记忆因此获得新的权限。

**工具授权**：针对某个工具动作、执行目标、目录、网络范围或依赖操作授予的有限许可。工具授权和工具记忆相互独立，授权不能由模型或历史记忆自行扩大。

**记忆遗忘**：用户主动要求降低或移除记忆影响的操作。遗忘可以只删除派生长期记忆，也可以连同原始事件和派生索引一起彻底删除，二者必须在界面上明确区分。
