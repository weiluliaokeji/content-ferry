# 文渡待开发与优化事项

本文件只记录已经确认、但当前版本尚未实现的事项。已完成的功能不再保留在这里；产品规则和已交付能力以 `spec/`、`README.md`、`docs/USER-GUIDE.md` 和代码为准。

## 研究资料的来源可信度与精确引用

**状态：** 后续增强

- 支持用户可配置的官方来源域名白名单，并在资料卡中显示“官方/公开/手工/实验观察”等可信度提示。
- 将资料卡中的具体主张映射到正文段落或句子，支持查看来源、定位引用和重新核查。
- 对必须登录、验证码或复杂动态页面的来源，继续完善可见浏览器接管和逐来源步骤记录。

**完成条件：** 资料卡能明确说明来源类型、核查状态和获取时间；正文中的事实可定位到具体资料卡；遇到登录或验证码时交给用户，不伪造已核查结果。

## 记忆维护的高级能力

**状态：** 后续增强

- 为巩固、压缩、合并和遗忘任务增加更细粒度的 watermark，支持按作用域展示恢复进度和失败任务。
- 为互相冲突的账号、系列和平台记忆增加版本比较与用户确认界面。
- 在记忆规模明显增长后，再评估 FTS5/trigram 或其他检索优化；当前 SQLite 作用域检索已满足基础闭环。

**完成条件：** 维护任务失败不会推进错误 watermark；冲突版本和来源可查看；停用或删除的记忆不再进入模型上下文；文章正文不受记忆维护影响。

## 阿文工具调用闭环

**状态：** 已实现；后续增强为更细粒度事件投影和更多工具适配器

当前版本由阿文提出结构化执行计划。`ToolWorkflowRunner` 已实现模型回合、结构化请求校验、权限暂停/恢复、拒绝后重规划、取消/中止传播和事件记录；阿文对话已接入低风险网页检索、系统工具发现、文章库只读和高风险 Git 授权暂停，工作流快照、请求元数据和工具结果也已写入执行记录。编辑器右侧“执行活动”显示目标、参数、权限、事件、结果和失败原因；授权卡只在右侧活动中出现，底部对话只提供跳转。应用设置可配置阿文专属工作区，Git staging 固定在其 `git-sources/` 子目录，工作区内不重复询问目录边界，越界目标直接拒绝；失败工作流不会静默回退旧直答路径。应用重启后，带待授权请求的中断工作流可恢复为“重新授权”，其他中断工作流沿用原记录显式重新规划，不会自动重放。低风险、已授权且未扩大范围的读取可连续自动执行；跨目录、跨网络、安装、删除、写入、外部副作用或执行目标边界变化时暂停并询问。

新建草稿可直接吸收低风险工具结果但保留来源链；编辑已有文章只生成用户可应用的建议。Git 结果可在右侧活动中填写主张并保存为待核验证据，也可单独采纳为证据卡；“应用为文章建议”仍需补齐独立动作，不能与保存或采纳合并。拒绝某个操作后允许阿文在不执行该操作的前提下重新规划替代方案，但替代方案必须重新经过权限判断。

**完成条件：** 工具调用、权限询问、执行记录、取消/中断、替代方案和结果回传可追溯；模型不能绕过权限策略、扩大目录/网络范围或把实验观察直接当成事实；用户能在执行活动中看见当前目标、动作、风险、进度和最终结果。

## 通用联网找图闭环

**状态：** 候选搜索、文章级搜图历史、Codex 视觉初审、人工确认插入和阿文受控 `find_images` 工具已完成；自定义视觉连接仍在开发

已在“技能与模型”提供关闭、跟随阿文或指定模型连接三种图片初审配置，并复用现有 Tavily 与模型凭据；编辑器已提供 Tavily 候选搜索、按文章保存最近 30 次搜索历史、来源展示、Codex 视觉初审和用户确认后下载插入。阿文现在可在明确找图意图下请求应用侧 `find_images`，候选会写入对话和搜图历史并打开图片面板；没有视觉模型时必须保持规则筛选降级。后续按 GitHub Issue #21 增强自定义视觉连接。

**完成条件：** 候选来源可追溯；图片在用户确认前不写入文章；初审失败不丢失候选；下载继续使用现有 SSRF 防护和文章素材存储；阿文不能绕过用户确认直接改正文。

## 代码取证的符号级分析

**状态：** 后续增强

在当前 Git 文件/行号摘要基础上，按需增加函数、类、调用关系和配置项级分析。新增能力必须继续使用已确认的目录、网络、依赖和执行权限，不得扩大模型权限。

**完成条件：** 分析结果保留 commit、文件、行号和摘要来源；超出读取上限或无法解析时明确提示，不把推断当成源码事实。

## 普通 Markdown 内容源的扩展约定

**状态：** 后续增强

普通 Markdown 内容源的基础扫描、编辑、图片处理、Front Matter 映射和持久化已经完成。后续如需支持非 `<文章目录>/index.md` 的入口、更多 Front Matter 方言或多级目录规则，先补充需求和迁移兼容方案，再实现并增加回归测试。

## 微信视频号短视频工作流

**状态：** 第一阶段已完成；文渡原生执行规划中

- `assets/skills/wechat-video-creator/` 已纳入仓库，保留脚本、模板、参考资料和生图子技能，供千问办公、WorkBuddy 等外部智能体读取和执行。
- 当前不会把它加入 `assets/skills/manifest.json`，也不会在文渡界面展示为已经支持的原生视频生成能力。
- 后续实现文渡原生执行时，需要增加可恢复的视频任务、环境预检、用户确认、凭据安全存储、脚本/渲染适配器和产物回填；不能让模型直接绕过执行权限拼接任意命令。
- 真实 `config/voice_cache.json`、Python 缓存和渲染调试帧不属于技能源文件，不得提交或打包。

**完成条件：** 外部智能体可以从仓库技能目录完成现有视频工作流；文渡原生执行有独立任务状态、取消/重试、日志、凭据隔离和人工审核节点，且失败不会丢失已生成产物。
