# Do not put AppSecret, callback Token, EncodingAESKey, Cookie or Gitee token in Git.
# Production credentials will be stored through the Windows credential adapter.

# Optional local development overrides only.
CONTENTFERRY_DATA_DIR=
CONTENTFERRY_DEV_SERVER_URL=http://127.0.0.1:5173
